
*->  fun type=root name="coder" id="coder" scop=root -> act == crt [code, data, file]
      -> crt spec lng=VibeDSL proto:
      	-> exam(spec:syn==VibeDSL:dict:syn)<>:
        	-> exam(spec:logic!=?)<>:
            		-> show spec lang=VibeDSL src=orig with(VibeDSL:prnt:strk:->add(ref lang=usr:lang)):
               			-> usr apr:
                  			-> crt [code, data, file] lng = spec:lng:name:
                     			-> exam(file:syn==spec:lng:syn): 
                        			-> exam(file:logic==spec:logic)<>:
                           				-> wrt:goal
                           				-> retry(5)!:rollb
                     			-> retry(5)      
            	-> retry(5)
         -> show(varn(varn5)):retry


 *->  fun type=root name="dsl-plan" id="dsl-plan" scop=root -> act == crt [plan] lng=VibeDSL
      -> crt spec lng=VibeDSL blplan(plan):
         -> exam(spec:syn==VibeDSL:dict:syn)<>:
            -> exam(spec:logic!=?)<>:
               -> show spec lang=VibeDSL src=orig with(VibeDSL:prnt:strk:->add(ref lang=usr:lang)):
                  -> usr apr:
                     -> wrt plan:goal
                     -> varn(varn5):retry
            -> ask:retry
         -> ask:retry